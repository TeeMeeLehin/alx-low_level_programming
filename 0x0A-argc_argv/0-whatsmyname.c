#include "main.h"
/**
* _print_name - prints the name of the program
* @argc: the number of arguments passed
* @argv: the array of argument strings passed
*
* Return: int
*/
int _print_name(int argc, char *argv[])
{
printf("%s", argv[0]);
return (0);
}

